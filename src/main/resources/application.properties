spring.datasource.url=${DB_URL:${DATABASE_URL:jdbc:postgresql://localhost:5432/seyahat_db}}
spring.datasource.username=${DB_USERNAME:${DATABASE_USERNAME:seyahat_user}}
spring.datasource.password=${DB_PASSWORD:${DATABASE_PASSWORD:seyahat123}}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.web.cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:3000}
server.port=${PORT:8080}
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=never
app.jwt.secret=${JWT_SECRET:seyahat-planlayici-gizli-anahtar-123456789-very-secure}
app.jwt.expiration-ms=${JWT_EXPIRATION_MS:86400000}